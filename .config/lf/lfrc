# Setting
set nodrawbox
set info size
set ifs "\n"
set filesep "\n"
set previewer preview
set cleaner preview-image

# Key bindings
map <enter> open
map gh cd ~
map gr cd /

# Commands
cmd open $ open $fx

map <c-s> %lf-rsync-here
