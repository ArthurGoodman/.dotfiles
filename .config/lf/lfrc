# Setting
# set nodrawbox
set info size
set ifs "\n"
set filesep "\n"
set previewer ~/.config/lf/previewer.sh
set cleaner ~/.config/lf/clear_img.sh

# Key bindings
map <enter> open
map gh cd ~
map gr cd /

# Commands
cmd open $ open $fx

map <c-s> %lf-rsync-here
